% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/svy_counts.R
\name{svy_count}
\alias{svy_count}
\title{Count Observations by Group}
\usage{
svy_count(data, vars, wt = NULL, .drop = FALSE, na_rm = TRUE, ...)
}
\arguments{
\item{data}{A data frame or tibble.}

\item{vars}{A character vector of variable names to group by.}

\item{wt}{Frequency weights. Default is \code{NULL}.}

\item{.drop}{Logical. If \code{FALSE} (default), empty groups are included in the output.}

\item{na_rm}{Logical. If \code{TRUE} (default), \code{NA} values in the grouping variables
are removed from the result before calculating frequencies.}

\item{...}{Additional arguments passed to \code{dplyr::count} (e.g., \code{sort}, \code{name}).}
}
\value{
A tibble containing the grouping variables, \code{n} (count), and \code{Freq} (percentage).
}
\description{
A wrapper around \code{dplyr::count} that adds survey-specific functionality:
\itemize{
\item Automatically calculates a percentage column (\code{Freq}).
\item Defaults to \code{.drop = FALSE} to show empty categories.
\item Automatically converts \code{haven_labelled} variables to factors (using
\code{levels = "both"}) to ensure all labels are counted.
}
}
