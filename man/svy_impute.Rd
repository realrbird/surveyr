% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/svy_impute.R
\name{svy_impute}
\alias{svy_impute}
\title{Impute Missing Values in Survey Data}
\usage{
svy_impute(
  data,
  vars_to_impute,
  vars_to_keep,
  pre_fix = "imp_",
  convert_to_fct = TRUE,
  method = "ranger",
  seed = NA,
  correlation_threshold = 0.99,
  ...
)
}
\arguments{
\item{data}{A data frame or tibble containing the survey data.}

\item{vars_to_impute}{A character vector of variable names to impute.}

\item{vars_to_keep}{A character vector of variable names to keep and return (imputed versions).}

\item{pre_fix}{A character string to prefix the returned variable names. Default is "imp_".}

\item{convert_to_fct}{Logical. If TRUE, converts labelled/character variables to factors before imputation. Default is TRUE.}

\item{method}{The imputation method to use (passed to \code{pewmethods::impute_vars}). Default is "ranger".}

\item{seed}{Numeric seed for reproducibility. Default is NA.}

\item{correlation_threshold}{Numeric value (0-1). Warns if variables are correlated above this threshold. Default is 0.99.}

\item{...}{Additional arguments passed to \code{pewmethods::impute_vars}.}
}
\value{
A data frame containing the original ID (if applicable) and the imputed variables merged back.
}
\description{
Imputes missing values for specified variables using the \code{pewmethods} package (which wraps \code{mice} and \code{ranger}).
It performs checks for variable existence, naming conflicts, and high correlation before imputation.
}
