% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weight_creation.R
\name{svy_trim_vector}
\alias{svy_trim_vector}
\title{Trim Extreme Survey Weights (Vectorized)}
\usage{
svy_trim_vector(
  weights,
  lower_quantile = 0.01,
  upper_quantile = 0.99,
  normalize = TRUE,
  print_output = TRUE,
  ...
)
}
\arguments{
\item{weights}{A numeric vector of survey weights.}

\item{lower_quantile}{The lower quantile (e.g., 0.01 for 1st percentile) below which
weights will be capped at the threshold. Defaults to 0.01.}

\item{upper_quantile}{The upper quantile (e.g., 0.99 for 99th percentile) above which
weights will be capped at the threshold. Defaults to 0.99.}

\item{normalize}{Logical. If \code{TRUE} (default), weights are rescaled to preserve
the original sum using \code{pewmethods::trim_weights}. If \code{FALSE}, strict numeric
caps are applied without rescaling.}

\item{print_output}{A logical flag. If \code{TRUE}, a diagnostic report containing
quantiles and stats is printed to the console. Defaults to \code{TRUE}.}

\item{...}{Additional arguments passed (e.g., \code{minval}, \code{maxval}, \code{strict}).}
}
\value{
A \strong{numeric vector} containing the final trimmed weights.
}
\description{
Implements weight trimming (Winsorization) to reduce the influence
of extreme weights. Operates directly on a numeric vector.
}
